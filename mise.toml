[tools]
node = "26"

[tasks.install]
description = "Install npm dependencies"
run = "npm install"

[tasks.build]
description = "Build the extension into dist/"
run = "npm run build"

[tasks.typecheck]
description = "Type-check the project"
run = "npm run typecheck"

[tasks.test]
description = "Run unit tests"
run = "npm test"

[tasks.package]
description = "Package dist/ for Chrome Web Store (unlisted, version from manifest)"
run = "bash scripts/package.sh"

[tasks.ci]
description = "Run the full CI gate: typecheck, tests, build"
run = ["npm run typecheck", "npm test", "npm run build"]
