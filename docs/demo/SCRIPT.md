# CANChat Agent — demo video script

Generated by `npm run demo:record` — the timecodes below are measured
from the assembled video (`docs/demo/canagent-demo.mp4`), so this script always
matches the finished cut. Narration lives in `scripts/demo/narration.ts` as
per-scene beats anchored to on-screen checkpoints; edit there and re-record.

| Time | Scene | Narration | On screen |
|---|---|---|---|
| 00:00–00:15 | **Title card** | CANChat Agent is an A I agent that lives in your browser’s side panel — and uses the browser itself as its toolset. Over the next few minutes we’ll set it up from scratch, on real pages, and walk through every major feature. | Branded title card. |
| 00:15–00:47 | **First run — connect a model** | On first run, the panel shows a short welcome beside whatever you’re reading — here, the Wikipedia article on the Rideau Canal. It asks for just three things: an endpoint, a key, and a model. Any OpenAI-compatible endpoint works — a cloud A P I, a local model, or your organization’s gateway. The key is stored only on this device, never synced. Test connection sends one tiny request, and reports plainly that it worked. Save and start — and that’s the whole setup. | Live Wikipedia page on the left; onboarding card in the panel: fill three fields, Test connection, Save & start. |
| 00:47–01:12 | **Ask about the current page** | The simplest thing you can do: ask about the page you’re on. The question goes in the composer in plain language. Watch the status pill — thinking, then acting, as the agent reads the tab. And the answer is a genuine summary of this article — the canal’s length, its heritage status, the winter Skateway — with a one-click copy button and the source cited. | Types a question; the agent reads the live article and answers with a real summary. |
| 01:12–01:46 | **Research with a live plan** | For bigger tasks, the agent plans in the open. Let’s ask it to compare Canada’s historic waterways. It lays out its plan first — four steps, each ticked off as it completes. Then it opens real sources — watch the tab strip: the Northwest Passage and the Trent–Severn Waterway open as live tabs, gathered into this conversation’s tab group. The synthesis draws on every tab it opened, and lists them as sources. And the tool activity log keeps the full trace — every call the agent made, in order. | A research task: plan appears, real tabs open across the fake tab strip, synthesis cites all sources; tool log expanded. |
| 01:46–02:16 | **Approvals — you stay in control** | Now the most important design decision: consent. Running code inside a page changes state, so the agent stops and asks first. The card leads with a plain-language reason; the mechanics sit under the technical-detail toggle. Nothing outbound ever happens silently. Approve it, and the action runs — the agent reads and reports this page’s real title. Deny it, and nothing happens at all. | A state-changing action raises the approval card; Approve runs it; the answer reports the page’s real title. |
| 02:16–02:44 | **Knowledge bases** | Knowledge bases are on-device document stores. In the workspace, drop in files — or index whole folders. This briefing note is parsed and embedded right on the machine. Nothing is uploaded anywhere. Back in the panel, a hash sign references the base by name. The agent searches the note and answers from it — the canal’s navigation season, with the source file cited. | Workspace Knowledge page: upload a briefing note; back in the panel, a # reference searches it and the answer cites the note. |
| 02:44–03:12 | **History, undo, and the More menu** | Every conversation is saved automatically — there is no save button to forget. Each thread gets a generated title and a one-line summary, with search, sorting, and colour labels. The three-dot menu holds the everyday extras: the text-size control, saving the conversation as a file, undoing the last exchange, and learn mode. And New Chat starts fresh — the old thread stays safely in history. | History overlay with generated title and summary; the ⋯ More menu with text-size, save, undo, learn mode. |
| 03:12–03:27 | **Skills and app playbooks** | Skills are procedures you teach the agent once and reuse forever — written by hand, imported, or installed from the shared playbook library. Each one becomes a slash command: type a slash in the composer and pick it from the menu. | Workspace Skills page with the seeded skills; back in the panel, slash-command autocomplete. |
| 03:27–04:02 | **The Workspace console** | The settings gear opens the workspace — a full tab with a page for everything. Models holds the connection and every advanced option. Behaviour, generation, embeddings, connected services — one page, one scroll. Memory shows what the agent has learned about you — searchable and fully editable. Automations runs scheduled tasks and site triggers unattended — here’s this morning’s news brief, run on schedule. And the files those runs produce land in Products, kept on-device and ready to download. | Models page with the Advanced section scrolled through; then Memory, Automations (seeded run history), Products. |
| 04:02–04:19 | **Documents out** | The agent produces real files, not just chat. Ask for a three-slide deck on this article… …and a PowerPoint is built on-device — titles, bullets, and a speaker note — delivered as a download card right in the conversation. | Asks for a three-slide deck on the article; a .pptx download card appears in the chat. |
| 04:19–04:37 | **Built for imperfect networks** | One more thing: failure handling. This request is about to hit a rate-limited endpoint. The agent reads the server’s retry hint, backs off, and says so — right in the conversation. Then it recovers to a clean answer on its own. No babysitting. | A request against a rate-limited endpoint: retrying notice, then a clean recovered answer. |
| 04:37–04:50 | **Wrap-up** | That’s CANChat Agent: your browser, your session, your data — with an agent that shows its plan, logs its tools, and asks before it acts. Load the extension, connect a model, and try it on your own tabs. | Outro card with the project location. |

Total running time: **04:50**.
